# Binaries
*.exe
*.dll
*.so
*.dylib

# Test files
test-input.json
test_input.json
*.txt
*.html
test.cmd
test.ps1

# Debug files
nul
statusline.debug

# Local Claude settings only (settings.json is OK to commit)
.claude/settings.local.json

# User-local statusline config (proxy, cache tuning). Keep the .example.yaml
# tracked as the template; the actual .yml/.yaml stays per-machine.
.claude/statusline.yml
.claude/statusline.yaml

# Go
go.work
vendor/
*.out
*.coverprofile

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
